# 🗺️ QUESTBOOK: Anchor Marianas — first stranger dollar + the IDI proof

Two real tracks, running in parallel, competing only for Adam's hours, not
for each other. The revenue track is the urgent one: `EVIDENCE.md` still
shows a literal $0 in verified stranger revenue for anchormarianas.com, and
that's the actual boss of this run. The IDI track is real too, just not
time-pressured the same way — free, mom-approved, no deadline.

Line 2 (the Guam silver-generation eldercare rollup) has its own dedicated
tracker already: `NORTH_STAR.md` at this project's root, 3/10 rungs, next
action is a real phone call. Not duplicated here — check that file directly
for that thread.

---

## ⚔️ MAIN QUESTLINE — first real stranger dollar

- ✅ **Q1. Prep the dentist outreach batch** : 4 personalized openers
  written, real names/phones/villages, listing-based (honest about no
  review data yet). `.anchor/eod-war-plan-2026-08-09-dentists.md`
- 🔓 **Q2. Actually send the 4 dentist messages** : A1 Bourgeois Kenny DDS,
  A2 Hafa Adai Family Dental, A3 ISA Dental, A4 Mangilao Dental Clinic.
  This is the one nothing else unlocks without — a prepared script isn't
  outreach.
  Reward: real replies to work with, the EOD scoreboard stops sitting at
  zero.
- ⬛ **Q3. Log every reply within 5 min, run a real AnchorScan read on
  first "yes"** : never paste a fabricated review-based read — if Apify
  still isn't configured, say "still gathering your review data" rather
  than inventing content.
- ⬛ **Q4. Pitch the $149/mo AI Receptionist using the real vertical math**
  : "8-10 additional appointments/month × ~$150 avg visit = $1,200-$1,500
  incremental monthly revenue" (the sourced number from this session's
  fastest-time-to-dollar research, not invented for this pitch).
- ⬛ **👑 BOSS: first verified stranger payment lands** : updates
  `EVIDENCE.md`'s Baseline table off its current $0, with a Stripe-verified
  receipt, not a claim.

## ⚔️ PARALLEL QUESTLINE — the IDI proof-of-value case study

Unpaid by design. Real, family-approved, no clock on it. Practice ground
for complex bespoke automation before it ever touches a paying stranger.

- ✅ **Q1. Get the yes** : free AI-automation work on IDI's own operations,
  approved by a real decision-maker there (Adam's mom). Logged
  `decisions-log.md` 2026-08-10.
- ✅ **Q2. Name the first real target** : order processing, named directly
  as the slowest part of IDI's operations right now.
- 🔓 **Q3. Scope the actual current workflow end to end** : who touches an
  order, what tools/steps are involved, where the real delay actually is.
  Not yet done — this is the next real grounding step, and no fix or
  time/money estimate gets proposed before it.
- ⬛ **Q4. Propose and build the first automation fix** : scoped from Q3's
  findings, not from assumption.
- ⬛ **Q5. Measure real time/money saved** : a real before/after number,
  same no-invented-numbers discipline as everywhere else in this repo.
- ⬛ **👑 BOSS: a provable IDI case study** : becomes the pitch for paid
  Fractional AI Engineer work at comparable Guam wholesalers/distributors
  (MASTERPLAN.md, Line 1).

## ⚔️ SMALL CLOSING CHAIN — GitHub org migration

Almost done, one real step left.

- ✅ **Q1. Org created, both repos transferred** :
  `anchormarianas-com/anchormarianas.com` and `.../idiguam.com`, local
  remotes updated, verified with `repo-connect` (65/65 claims, 0 broken).
- ✅ **Q2. Found and fixed the broken production build** : PR #48 merged,
  restored `components/structured-data.tsx`, verified via local build (all
  24 pages generate).
- 🔓 **Q3. Grant the Vercel GitHub App access to the new org** :
  [github.com/apps/vercel](https://github.com/apps/vercel) → Configure →
  add `anchormarianas-com`. Requires Adam directly, an app-authorization
  action.
- ⬛ **Q4. Reconnect and confirm a real production deploy actually ships**
  : `vercel git connect` once Q3 clears, then verify a fresh deployment
  reaches `READY`, not `ERROR`.

---

## 📜 RULES OF PLAY

- ⬛ locked quests stay locked, no skipping ahead.
- One main-quest step at a time per track — two tracks may run in
  parallel, but don't split focus inside a single track.
- The revenue track's boss is the real priority this week; the IDI track
  has no deadline and should not be rushed just because it's unlocked.
- Never paste a fabricated review-based read, ever, regardless of how
  badly Q3 (Main) wants to move — this is the one hard rule that doesn't
  bend for speed.
- No em dashes, anywhere, ever (repo-wide discipline, applies here too).
