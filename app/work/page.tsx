import Image from "next/image"
import Link from "next/link"
import work from "@/content/work.json"
import { AgencyHero, AgencyCTA } from "@/components/agency"
export const metadata = { title: "Business Websites & Apps: Our Work | Anchor Marianas", description: "Delivered website and app work for Hilton Guam and International Distributors, Inc.", alternates: { canonical: "/work" } }
export default function WorkPage() {
  return <div className="agency-page"><AgencyHero eyebrow="Our work" title="Selected work."  /><section className="agency-shell agency-section agency-section-short"><article className="agency-card"><p className="agency-eyebrow">Hospitality · Paid engagement</p><h2>Hilton Guam</h2><p className="agency-lead">Website and gym app work delivered by Adam.</p></article>{work.caseStudies.map(cs => <article className="agency-card agency-work-card" key={cs.slug}><p className="agency-eyebrow">Wholesale distribution · {cs.location}</p><h2>{cs.client}</h2><p className="agency-lead">{cs.summary}</p><p className="agency-small">{cs.engagement}</p><Image src={cs.images[0].src} alt={cs.images[0].alt} width={1440} height={900} className="w-full h-auto" /><h3>What we delivered</h3><ul className="mt-5 grid gap-3 sm:grid-cols-2">{cs.deliverables.map(d => <li key={d} className="text-sm text-muted-foreground">{d}</li>)}</ul><a className="agency-text-link" href={cs.liveUrl} target="_blank" rel="noopener noreferrer">Visit the website ↗</a></article>)}<p className="agency-small">Have a similar project in mind? <Link href="/contact">Tell us about your business.</Link></p></section><AgencyCTA /></div>
}
